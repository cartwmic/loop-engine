#!/usr/bin/env bash
# T012-only: publication-mode smoke for the fixed foundation parent / plan candidate pair.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
PARENT="${PARENT_REVISION:-7552af5968b4a2c10aefd01fbfa6c351817e1b8b}"
CANDIDATE="${CANDIDATE_REVISION:-581c23bb085718d37d994d77d59d3d70b7ea309f}"
TIMEOUT_SECONDS="${TIMEOUT_SECONDS:-900}"
exec python3 "$ROOT/request_builder.py" \
  --publication-smoke \
  --parent "$PARENT" \
  --candidate "$CANDIDATE" \
  --timeout-seconds "$TIMEOUT_SECONDS"
