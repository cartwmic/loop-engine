schema_version = 2

[defaults]
timeout_seconds = 30
max_output_bytes = 4096

[runner]
inputs = ["quality/manifest.toml"]

[[checks]]
id = "test"
phases = ["pre-commit"]
scope = "repository"
program = "cargo"
args = ["test", "--workspace", "--locked"]
cwd = "{candidate_root}"
