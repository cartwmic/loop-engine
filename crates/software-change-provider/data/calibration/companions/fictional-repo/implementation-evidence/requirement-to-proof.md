# Requirement-to-proof matrix

This matrix supports authoritative [authoritative product PRD](../docs/PRD.md). It maps four exact acceptance requirements to named executable validation proof. `fictional-repo/scripts/assert-requirement-proof.py` checks this exact mapping against supplied repository-state proof inventory. Provider tests prove deterministic mechanics; supplied review records and this matrix do not establish semantic review quality.

| Requirement | Truthful named proof | Boundary |
|---|---|---|
| A caller can inspect the frozen policies and artifact schemas before evaluating a transition. | `fictional-repo/provider/tests/a1.rs::standard_run_progresses_schema_deny_then_evidence_deny_then_allow`; `fictional-repo/provider/tests/a15.rs::every_shipped_profile_starts_and_exposes_policies_and_schemas`; `fictional-repo/provider/tests/describe_protocol.rs::describe_matches_committed_snapshot_byte_for_byte`; `scripts/production-journey.py::frozen_profile_is_inspectable_before_transition`. | The public journey observes caller-selected frozen `initial_input` before transition evaluation and again after events; flat `describe` remains exact static workflow topology only. |
| A malformed subject artifact is denied with each violated structural rule named. | `schema::tests::instance_collects_object_rules_and_nested_properties` proves exhaustive checker output; `fictional-repo/provider/tests/evaluate.rs::schema_deny_reports_every_simultaneous_structural_violation` proves checker-to-denial projection preserves every exact path/rule pair in deterministic order; existing `absent_artifact_is_schema_deny`, `unparseable_artifact_is_schema_deny`, `revision_link_mismatch_is_schema_deny_naming_both_artifacts`, and `schema_deny_is_byte_identical_when_only_context_varies` cover adjacent structural denial cases; `scripts/production-journey.py::malformed_artifact_denial_names_all_rules` observes all simultaneous path/rule diagnostics through the public caller path. | Structural denial precedes evidence satisfaction and leaves state unchanged; evidence bytes do not prove artifact meaning. |
| A checked transition refuses stale, self-authored, duplicate-author, or configured-obligation-incomplete evidence. | `fictional-repo/provider/src/evidence.rs::stale_revision_does_not_satisfy_and_names_both_revisions`; `stale_config_does_not_satisfy_and_names_both_versions`; `subject_author_identity_requires_exact_name_and_kind_pair`; `n_two_requires_distinct_non_subject_authors_and_rejects_standing_fail`; `fictional-repo/provider/tests/evaluate.rs::evidence_phase_denies_with_configured_axis_diagnostics`; `scripts/production-journey.py::evidence_denial_reports_each_configured_reason`. | The public journey observes every configured denial reason and unchanged state before conforming evidence allows progression. Exact declared identity is deterministic but unauthenticated; external process/provenance controls remain outside scope. |
| The reference workflow reaches its terminal state only after its configured validation obligations are satisfied. | `scripts/production-journey.py::terminal_validation_gate` reads the frozen validation-adversarial-review policy IDs and author counts, asserts the initial denial names every missing policy and leaves that review state unchanged, validates each appended record's gate, policy, revision, config, result, non-subject identity, and distinct-author count against those obligations, then asserts the accepted transition to end, final lifecycle, no requestable events, and retained denial lineage; `python3 fictional-repo/scripts/assert-doc-authority.py` checks authority, exact acceptance coverage, and relative Markdown links; provider transition mechanics are covered by the first requirement's a1 proof. | Synthetic records exercise provider mechanics only. External semantic review remains outside provider execution and is not attested here. |

Document integration proof reads the supplied companions named by validation coverage: root README, provider README, authoritative PRD, supplemental change PRD, technical design, task packets, this matrix, `fictional-repo/scripts/assert-doc-authority.py`, and `fictional-repo/scripts/assert-requirement-proof.py`. `fictional-repo/docs/PRD.md` is sole product authority; the change-scoped PRD is supplemental/non-authoritative. Validation report / docs-integrated review is final validation consumer.
