{
  "schema_version": 1,
  "profile_version": "agents-2",
  "mode": "audit",
  "target": {"id": "AGENTS.md", "path": "/abs/path/to/AGENTS.md"},
  "deterministic_policies": [
    {"id":"document-present","type":"non-empty"},
    {"id":"scope-authority","type":"required-heading","aliases":["Scope","Authority","Scope and Authority","Repository Scope","Instruction Scope"]},
    {"id":"workflow-validation","type":"required-heading","aliases":["Workflow","Development Workflow","Working in This Repository","Validation","Testing"]},
    {"id":"completion-handoff","type":"required-heading","aliases":["Completion","Definition of Done","Done","Handoff","Completion and Handoff"]},
    {"id":"workflow-command","type":"command-in-section","section_aliases":["Workflow","Development Workflow","Working in This Repository","Validation","Testing"]},
    {"id":"local-references","type":"local-references-resolve"}
  ],
  "semantic_policies": [
    {"id":"success-path-completeness","description":"An unfamiliar agent can follow a complete successful work path.","example_prompt":"Review success-path completeness for a fresh capable agent."},
    {"id":"operational-precision","description":"Work and validation instructions are executable and precise. Commands are copy-pasteable with flags, scoped (single crate or single test versus full suite), and tied to when to run them so a fresh agent can close the loop without a human.","example_prompt":"Review operational precision. Confirm commands are copy-pasteable with flags, scoped, and tied to when they run. Fail prose-only verification, missing flags that the repo requires, or a suggestion where a gate is needed. Use the tree to check that named commands exist."},
    {"id":"authority-resolution","description":"Scope and instruction authority are resolved without contradiction. Root versus nested AGENTS.md versus skills versus PRD versus README has one precedence order. Duplication of a skill or README body fails; a one-line pointer with when to load passes. Operator traps live here; product purpose and install live in README.","example_prompt":"Review authority resolution and precedence against the repository. Fail contradictory instructions, duplicated skill or README bodies, or operator traps parked only in README. A pointer with when-to-load is a pass. Use nested AGENTS.md files and skills as evidence."},
    {"id":"risk-boundary-sufficiency","description":"Repository-specific safety boundaries relevant to work are covered as always, ask first, or never. Critical never-rules that a tool can enforce are acknowledged as belonging in hooks or CI, not only markdown.","example_prompt":"Review repository-specific risk boundaries. Confirm always / ask first / never guidance scoped to this repo (secrets, generated workflows, main, provider TOML, run databases). Fail a never-list with no ask-first tier when the repo has ambiguous dangerous actions, or never-rules that a hook or CI could enforce with no acknowledgment that markdown is advisory. Use the tree as evidence."},
    {"id":"completion-handoff","description":"Completion evidence and durable handoff information are precise.","example_prompt":"Review completion evidence and durable handoff."},
    {"id":"non-discoverable-sharp-edges","description":"The file names repository-specific traps a capable stranger would hit and how to navigate each one. Each named trap has a concrete Do (command, path, or procedure). Unpaired Don'ts fail. If no such traps exist, that conclusion is explicit and tree-grounded. Architecture tours and language defaults do not satisfy.","example_prompt":"Compare this AGENTS.md to the repository. List non-obvious, sharp, or easy-to-misuse behaviors a fresh capable agent would hit (CLI, topology, evidence, generated files, fixtures, authority, env not in help). For each, the file must name it and give a navigable Do. Unpaired Don'ts fail. Architecture tours and language defaults do not satisfy. If no such traps exist, say so explicitly from the tree."},
    {"id":"ambiguity-resolution","description":"Where the repository has two reasonable ways to do a thing, the file forces the choice up front (table or ordered rule), not a prose maybe.","example_prompt":"Compare this AGENTS.md to the repository. Where two reasonable ways exist (two providers, two journeys, draft versus audit, source versus packaged, crate AGENTS versus root), the file must force the choice. Fail a prose maybe. If no such forks exist, say so explicitly from the tree."},
    {"id":"signal-density","description":"Every always-on instruction is universal enough to belong in every session. Discoverable layout, linter-enforceable style, generated overviews, and tutorials are absent or are pointers (skill, nested AGENTS.md, docs) with a one-line when-to-load. The file is lean relative to the module it covers.","example_prompt":"Review signal density against the repository. Fail generated init dumps, file-by-file tree, fifteen or more unpaired don'ts, open-ended see-also that invites reading the whole docs tree, or restating language defaults. Pointers with when-to-load pass. Do not fail a dense file whose density is forced by real traps."},
    {"id":"living-config","description":"Content that would rot (commands, topology, argv) matches the tree now. Stale generated claims fail. The file does not accumulate hotfix rules from one bad session. Stale copies of commands that exist in a skill or CI file fail.","example_prompt":"Compare this AGENTS.md to the repository, skills, and CI. Fail commands, paths, or topology that do not match the tree now, and stale copies of commands that live elsewhere. Do not fail historical notes that are clearly dated and non-operative."}
  ]
}
